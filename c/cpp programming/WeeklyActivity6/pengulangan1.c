#include <stdio.h>
int main(){
    int x = 10;
    while (x>0)
    {
        printf("%i\n", x);
        x-=1;
        if (x == 0)
        {
            printf("Meluncur");
        }
        
    }

    
}